"""V-NAND distribution similarity search.

The public package intentionally exposes only its version for now. Pipeline
components live in focused modules so handcrafted embeddings can later be
replaced by learned image and curve encoders without changing the CLI.
"""

__version__ = "0.1.0"
